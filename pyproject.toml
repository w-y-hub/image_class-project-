[project]
name = "image-course"
version = "1.0.0"
description = "人脸美化系统 — 图像识别实践课程设计"
requires-python = ">=3.10, <3.13"
dependencies = [
    "numpy>=1.24",
    "opencv-python>=4.8",
    "Pillow>=10.0",
    "tensorflow>=2.15,<2.19",
]

[tool.uv]
# dev-dependencies replaced by dependency-groups per uv deprecation
