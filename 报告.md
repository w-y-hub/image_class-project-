<style>
body {
    font-size: 22px;
    line-height: 1.6;
}

h1 {
    font-size: 2.4em;
}

h2 {
    font-size: 2.0em;
}

h3 {
    font-size: 1.6em;
}

table {
    font-size: 1.1em;
}
</style>
# 基于 Python 的人脸美化系统

> 课程设计汇报 · 图像识别实践

---

## 一、选题

本课程设计选择 **题目一：基于 Python 的人脸美化系统实现**。

人脸美化在日常生活中随处可见——美颜相机、短视频滤镜、直播美颜、虚拟试妆。这些应用的本质，是图像处理与模式识别技术的综合运用。

本选题与课程内容高度契合，既能体现**滤波、增强、边缘检测、形态学操作**等基础图像处理知识，又能延伸至**人脸检测、局部区域处理、深度学习妆容迁移**等进阶内容。

---

## 二、研究背景

### 2.1 技术背景

**数字图像处理**是计算机视觉的基础。图像去噪、增强、锐化、边缘检测等经典方法至今仍是图像分析的核心手段，具有实现简单、运行高效、可解释性强等优点。

**人脸检测**是人脸相关应用的前置步骤。OpenCV 的 Haar Cascade 分类器基于 Viola-Jones 框架，使用 Haar-like 特征描述人脸区域，通过积分图加速计算，配合 AdaBoost 级联分类器快速筛选人脸位置。

**生成对抗网络（GAN）** 的发展推动了图像风格迁移技术的进步。BeautyGAN 能在一张人脸上应用参考妆容的风格，同时保留原始身份特征，体现了深度学习在人脸美化中的应用价值。

### 2.2 应用背景

| 场景 | 说明 |
|------|------|
| **美颜相机** | 磨皮、美白、亮眼、肤色调整 |
| **短视频 / 直播** | 实时美颜已成标配 |
| **社交媒体** | 提升上传照片的视觉效果 |
| **电商 / 虚拟试妆** | 妆容迁移提升交互体验与转化率 |

---

## 三、预期实现功能

### 3.1 基础界面

- 加载图片（jpg / png / bmp）
- 左右双栏显示原图和处理结果
- 保存结果、重置图像
- 底部状态栏 + 进度条反馈处理状态

### 3.2 图像处理功能

| 类别 | 功能列表 |
|------|---------|
| **去噪滤波** | 均值滤波、高斯滤波、中值滤波、双边滤波 |
| **图像增强** | 直方图均衡化、CLAHE 自适应均衡化 |
| **图像锐化** | 拉普拉斯锐化、Unsharp Mask 锐化 |
| **边缘检测** | Sobel 算子、Canny 算子（双阈值可调） |
| **形态学操作** | 腐蚀、膨胀、开运算、闭运算 |
| **人脸检测** | Haar Cascade 分类器 |
| **人脸美化** | 磨皮、肤色柔和、对比度增强、眼部增强 |

### 3.3 妆造迁移（BeautyGAN）

- 上传无妆人脸图片
- 从 4 种内置妆容模板中选择（日常淡妆、清新自然妆、韩系裸妆、晚宴浓妆）
- 或上传自定义妆容参考图
- BeautyGAN 自动完成妆容迁移

### 3.4 参数动态调节

选择不同功能时，界面自动显示对应的参数滑块。例如：

| 操作 | 显示的参数 |
|------|-----------|
| 均值滤波 | 滤波核大小 |
| Canny 边缘检测 | 低阈值、高阈值 |
| 人脸磨皮 | 磨皮强度、亮度提升 |
| 肤色柔和 | 柔和度 |
| 人脸检测 | （无参数，整区隐藏） |

---

## 四、设计思路

### 4.1 总体架构

系统采用**分层模块化架构**，自上而下分四层：

```
用户操作（点击 / 拖拽）
       ↓
  GUI 界面层（gui.py）
  交互、参数收集、文字图片化渲染
       ↓
  调度层（services.py）
  路由到对应算法模块
       ↓
  算法层（filters / face_detection / beauty / beautygan）
  执行具体图像处理
```

设计原则：
- **模块化**：每模块职责单一
- **松耦合**：GUI 不直接调用算法
- **可扩展**：新增功能只需在调度层加一个分支

### 4.2 模块职责

| 模块 | 职责 |
|------|------|
| `main.py` | 入口，设置编码、屏蔽 TF 日志 |
| `gui.py` | 图形界面，文字由 Pillow 渲染为图片 |
| `text_renderer.py` | 中文→图片渲染引擎 |
| `services.py` | 操作路由、中英文别名映射 |
| `filters.py` | 7 类基础图像处理算法 |
| `face_detection.py` | Haar Cascade 人脸检测 |
| `beauty.py` | 人脸美化（磨皮/肤色/对比度/眼部） |
| `beautygan.py` | 妆造迁移（TF 懒加载） |
| `image_io.py` | 图像读写，中文路径支持 |
| `utils.py` | 格式转换、显示缩放 |
| `template_manager.py` | 模板配置加载 |

---

## 五、项目运行

### 环境

- Python 3.10 ~ 3.12
- uv 包管理器

### 启动

```bash
uv sync
uv run python main.py
```

### 使用示例

1. 加载一张含人脸的图片
2. 选择"人脸磨皮"功能
3. 调整磨皮强度滑块
4. 点击"执行处理"，右侧显示结果
5. 满意后保存

---

## 六、总结与改进

### 当前成果

- ✅ 完整 GUI，8 类 20 种处理功能（含 BeautyGAN 妆造迁移）
- ✅ 人脸检测 + 局部美化
- ✅ 参数动态显示与标签自适应
- ✅ 跨平台中文显示（Pillow 图片化渲染）
- ✅ TF 懒加载，非 BeautyGAN 操作即时响应
- ✅ 内置 4 种妆容模板，支持自定义

### 改进方向

| 方向 | 说明 |
|------|------|
| **实时预览** | 拖滑块自动更新，省去点击"执行" |
| **DNN 人脸检测** | 更精准的深度学习检测模型 |
| **批量处理** | 一次处理多张图片 |
| **视频流** | 接入摄像头实时美颜 |
| **轻量模式** | 免 TF 安装，保留全部基础功能 |

---

_课程设计项目 · 图像识别实践_
