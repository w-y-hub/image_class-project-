# 基于 Python 的人脸美化系统 —— 课程设计报告

## 一、选题

本课程设计的选题为：**基于 Python 的人脸美化系统设计与实现**。

本项目围绕《图像识别实践》课程中的图像处理与模式识别内容，设计并实现了一个具有图形化界面的人脸美化系统。系统以 Python 为开发语言，结合 OpenCV、NumPy、Pillow 和 tkinter 等工具，实现了图像加载、显示、保存、重置，以及多种图像处理和人脸美化功能，同时扩展支持基于 BeautyGAN 的妆造迁移。

该选题与课程内容契合度高，既体现了图像基础处理方法的应用，也体现了人脸检测与智能图像生成技术的综合实践价值。

---

## 二、研究背景

### 2.1 技术背景

数字图像处理是计算机视觉领域的重要基础。图像去噪、图像增强、锐化、边缘检测和形态学处理等传统方法，仍然是图像分析与视觉应用中最常用的核心技术。这类方法具有实现简单、运行效率高、可解释性强等优点，非常适合课程设计项目实现。

在人脸相关应用中，人脸检测是后续人脸分析、美化和识别的前提。OpenCV 中的 Haar Cascade 分类器是一种经典的人脸检测方法，基于 Viola-Jones 框架，具有实现方便、速度较快的特点，适合本项目中使用。

随着深度学习的发展，基于生成对抗网络（GAN）的图像生成与风格迁移技术逐渐成熟。在人脸美化场景中，妆容迁移成为一个典型应用。BeautyGAN 能够将参考妆容图像中的风格迁移到目标人脸图像上，在保留原始身份特征的同时实现妆效变换，体现了现代视觉算法在人脸美化领域的应用价值。

### 2.2 应用背景

当前，人脸美化技术已经在实际生活中得到广泛应用，主要体现在以下几个方面：

- **美颜相机与拍照软件**：实现磨皮、美白、亮眼、提升肤色等功能；
- **短视频与直播平台**：实时美颜已成为常见需求；
- **社交媒体图像编辑**：提高用户上传照片的视觉效果；
- **在线试妆与电商平台**：通过妆容迁移提升交互体验和购买转化率。

因此，人脸美化系统不仅有较强的实际应用意义，也适合作为图像处理课程中的综合实践项目。

---

## 三、预期实现功能

根据课程设计要求，本系统预期实现并已基本完成以下功能。

### 3.1 基础界面功能

系统采用 tkinter 构建图形界面，具备以下基础操作能力：

- 加载本地图片，支持 jpg、png、bmp 等常见格式；
- 显示原图和处理后图像；
- 保存处理结果到本地；
- 重置图像到原始状态。

这些功能构成了系统的基本交互流程，也是后续图像处理功能实现的基础。

### 3.2 图像处理功能

本系统实现了多类图像处理操作，主要包括：

#### （1）去噪滤波
- 均值滤波
- 高斯滤波
- 中值滤波
- 双边滤波

#### （2）图像增强
- 直方图均衡化
- CLAHE（对比度受限自适应直方图均衡化）

#### （3）图像锐化
- 拉普拉斯锐化
- Unsharp Mask 锐化

#### （4）边缘检测
- Sobel 边缘检测
- Canny 边缘检测

#### （5）形态学处理
- 腐蚀
- 膨胀
- 开运算
- 闭运算

### 3.3 人脸检测与美化功能

在基础图像处理功能之上，系统进一步面向人脸图像场景，实现了以下功能：

- 基于 Haar Cascade 的人脸检测；
- 对人脸区域进行磨皮处理；
- 对人脸区域进行亮度提升；
- 对肤色进行柔和调整；
- 对全图进行对比度增强；
- 对眼部区域进行增强处理。

这一部分体现了局部区域处理（ROI）在实际图像应用中的重要性，也是本项目“人脸美化”主题的核心内容。

### 3.4 妆造迁移扩展功能

为增强系统的综合性和创新性，项目还加入了基于 BeautyGAN 的妆造迁移功能：

- 用户加载一张无妆人脸图像；
- 选择“妆造迁移（BeautyGAN）”功能；
- 弹窗选择一张妆容参考图像；
- 系统自动调用 BeautyGAN 模型进行推理；
- 将迁移结果显示并支持保存。

---

## 四、设计思路

### 4.1 总体设计思路

本系统采用**模块化设计思路**，将图形界面、图像输入输出、基础图像处理、人脸检测、人脸美化和工具函数分别封装在不同模块中，以提高代码结构的清晰性和可维护性。

系统的基本处理流程如下：

1. 用户通过图形界面加载图片；
2. 系统读取图像并显示原始图像；
3. 用户选择某种处理功能；
4. 程序调用相应算法模块对图像进行处理；
5. 处理结果显示在结果区域；
6. 用户可进一步保存结果或重置图像。

整个系统从“输入—处理—显示—保存”四个环节展开，便于用户操作，也方便功能扩展。

### 4.2 模块结构设计

根据项目结构，系统主要由以下模块构成：

#### 1. `main.py`
作为程序入口，负责启动整个人脸美化系统。设置 UTF-8 编码确保中文正常显示，并屏蔽 TensorFlow 运行时日志（`TF_CPP_MIN_LOG_LEVEL=3`、`ABSL_MIN_LOG_LEVEL=3`）。

#### 2. `modules/gui.py`
负责构建图形界面，包括：
- 主窗口布局；
- 控制面板与图像显示区域；
- 加载、保存、重置、执行处理等按钮事件；
- 单选按钮和参数滑块；
- 处理结果的实时更新显示。

#### 3. `modules/image_io.py`
负责图像读写功能，包括：
- 加载本地图像；
- 保存处理结果；
- 支持中文路径和多种图像格式。

#### 4. `modules/filters.py`
负责实现基础图像处理算法，包括：
- 均值滤波、高斯滤波、中值滤波、双边滤波；
- 直方图均衡化、CLAHE；
- 拉普拉斯锐化、Unsharp Mask；
- Sobel、Canny 边缘检测；
- 腐蚀、膨胀、开运算、闭运算。

#### 5. `modules/face_detection.py`
负责人脸检测功能，包括：
- 加载 Haar Cascade 分类器；
- 检测输入图像中的人脸区域；
- 绘制检测框用于调试和展示。

#### 6. `modules/beauty.py`
负责人脸美化相关功能。所有涉及人脸区域的操作均依赖 `face_detection.detect_faces()` 返回的人脸框。

| 函数 | 算法原理 | 色彩空间 | 参数 |
|------|---------|---------|------|
| `beautify_face_roi()` | 双边滤波保边去噪 + Y 通道亮度提升 + 与原图加权融合（原图 40% + 磨皮 60%） | BGR → YCrCb → BGR | `smooth_strength`（1~31）、`brightness`（-50~50） |
| `skin_softening()` | HSV 饱和度调整：降低饱和度方差，向平均饱和度靠拢 + 与原图混合 | BGR → HSV → BGR | `softness`（0.0~1.0） |
| `global_contrast_enhancement()` | 线性变换：`dst = src × alpha + beta` | —（全图操作） | `alpha`（0.5~2.0）、`beta`（-50~50） |
| `eye_enhancement()` | 根据人脸框估算眼睛区域（上半部 20%~50% 之间），Y 通道亮度提升 | BGR → YCrCb → BGR | `enhancement`（0.0~1.0） |

#### 7. `modules/services.py` — 统一调度层
负责接收 GUI 发来的操作指令，路由到对应的算法模块。

**设计特点**：
- 界面与算法解耦，GUI 不直接调用 filters、beauty 等模块
- BeautyGAN 采用懒加载，仅在使用时才导入 TensorFlow
- 支持中英文操作名自动映射

#### 8. `modules/text_renderer.py` — 中文文字渲染引擎
**职责**：将中文文字通过 Pillow + 项目自带字体渲染为图片，彻底解决 Linux 下 tkinter 中文乱码问题。

**关键功能**：
- `render_photo()`: 文字 → RGBA 图片 → tkinter PhotoImage
- `make_label()` / `make_button()` / `make_radio()`: 创建图片化的界面控件
- `make_option_menu()`: 创建中文下拉菜单
- `make_label_frame()`: 创建带渲染标题的分组框

#### 9. `modules/template_manager.py` — 妆造模板管理
**职责**：从 `assets/makeup_templates/templates.json` 读取妆造模板配置，验证图片文件存在性。

内置 4 种妆容模板：日常淡妆、清新自然妆、韩系裸妆、晚宴浓妆。用户也可选择自定义模板。

#### 10. `modules/utils.py`
提供辅助工具函数，包括：
- OpenCV 图像与 PIL 图像的格式转换；
- 图像缩放显示；
- 数值范围限制等。

### 4.3 设计特点

本系统具有以下设计特点：

1. **界面交互清晰**：左侧可滚动控制面板、右侧双栏显示原图和结果，底部状态栏与进度条实时反馈处理状态。
2. **模块划分明确**：每个模块职责单一，通过 services.py 统一调度，界面与算法解耦。
3. **参数动态调整**：选择不同操作自动显示对应参数滑块，标签文字随操作变化；无参数操作整个参数区自动隐藏。
4. **跨平台中文显示**：通过 text_renderer.py 将中文渲染为图片，绕过系统字体依赖；错误弹窗 `_safe_messagebox` 使用自定义 Toplevel 对话框，内容经 Pillow 渲染，无系统字体依赖。
5. **健壮的错误处理**：所有弹窗封装为 `_safe_messagebox`，窗口销毁后回退到 `print` 避免 TclError 二次崩溃；BeautyGAN 执行前对模板存在性和图片可读性做双重前置检查；main.py 设置 `TF_CPP_MIN_LOG_LEVEL=3`、`ABSL_MIN_LOG_LEVEL=3` 屏蔽 TF 日志噪音。
6. **TensorFlow 懒加载**：仅在选择 BeautyGAN 时导入 TF（3~8 秒），其余 20 种操作即时响应。

---

## 五、对应方法

### 5.1 去噪滤波方法

去噪滤波的作用是减少图像中的噪声干扰，使图像更加平滑，为后续处理提供更稳定的输入。

- **均值滤波**：使用邻域内像素的平均值替换中心像素，算法简单，但会导致边缘模糊；
- **高斯滤波**：根据高斯分布对邻域像素加权平均，保留边缘的能力优于均值滤波；
- **中值滤波**：使用邻域像素的中值替换中心像素，对椒盐噪声抑制效果较好；
- **双边滤波**：同时考虑空间距离和像素值差异，在平滑图像的同时较好保留边缘，适合磨皮处理。

对应实现模块：`modules/filters.py`

---

### 5.2 图像增强方法

图像增强用于提升图像对比度和视觉层次，使图像细节更加明显。

- **直方图均衡化**：重新分配灰度级分布，增强整体对比度；
- **CLAHE**：将图像划分为多个局部区域分别增强，并限制对比度，避免过度增强和噪声放大。

对应实现模块：`modules/filters.py`

---

### 5.3 图像锐化方法

图像锐化用于突出边缘和细节，使图像更加清晰。

- **拉普拉斯锐化**：利用二阶导数增强图像中灰度变化明显的区域；
- **Unsharp Mask 锐化**：先对图像进行模糊，再将原图与模糊图做差，提取细节后叠加回原图。

对应实现模块：`modules/filters.py`

---

### 5.4 边缘检测方法

边缘检测用于提取图像中灰度变化显著的区域。

- **Sobel 算子**：分别计算图像在水平和垂直方向上的梯度，用于提取边缘；
- **Canny 算法**：包括高斯平滑、梯度计算、非极大值抑制和双阈值检测等步骤，边缘检测效果更加稳定。

对应实现模块：`modules/filters.py`

---

### 5.5 形态学处理方法

形态学操作主要用于对图像结构进行调整，常用于边缘处理和噪声去除。

- **腐蚀**：缩小前景区域，消除小噪声；
- **膨胀**：扩大前景区域，填补小孔洞；
- **开运算**：先腐蚀后膨胀，适合去除小噪声；
- **闭运算**：先膨胀后腐蚀，适合填补小空洞。

对应实现模块：`modules/filters.py`

---

### 5.6 人脸检测方法

本系统采用 OpenCV 中的 **Haar Cascade 分类器** 进行人脸检测。

其原理主要包括：
- 使用 Haar-like 特征描述人脸区域；
- 借助积分图快速计算区域特征；
- 利用 AdaBoost 训练分类器；
- 通过级联结构快速筛除非人脸区域。

该方法运行效率较高、调用方便，适合课程设计项目的人脸检测需求。

对应实现模块：`modules/face_detection.py`

---

### 5.7 人脸美化方法

在人脸检测基础上，系统对人脸区域进行局部美化处理。

- **人脸区域磨皮**：利用双边滤波对人脸区域进行平滑处理，并结合亮度调整提升整体视觉效果；
- **亮度提升**：在 YCrCb 颜色空间中对亮度通道进行增强，使面部看起来更明亮；
- **肤色柔和**：在 HSV 颜色空间中调整饱和度，使肤色表现更加自然柔和；
- **全图对比度增强**：通过线性变换调整图像对比度和亮度；
- **眼部增强**：对眼睛区域进行亮度提升，使面部更有神采。

对应实现模块：`modules/beauty.py`

---

### 5.8 妆造迁移方法

本系统扩展使用 BeautyGAN 进行妆造迁移。

其基本思路为：
- 输入一张无妆目标人脸图像；
- 输入一张参考妆容图像；
- 调用预训练的 BeautyGAN 模型完成妆容风格迁移；
- 输出迁移后的结果图像。

BeautyGAN 属于基于生成对抗网络的图像风格迁移方法，能够实现更加高级的人脸美化效果，也体现了深度学习方法在图像处理中的应用。

对应模型位置：`modules/modelBeautyGAN/`

---

## 六、项目运行说明

### 6.1 环境要求

- Python 3.10 ~ 3.12
- uv 包管理器（自动管理 Python 版本与依赖）

### 6.2 主要依赖

- OpenCV 4.x
- NumPy
- Pillow
- tkinter
- TensorFlow 2.x（BeautyGAN 功能需要，懒加载）

### 6.3 安装与运行

```bash
# 安装 uv（如已安装则跳过）
curl -LsSf https://astral.sh/uv/install.sh | sh

# 一键安装所有依赖
uv sync

# 启动程序
uv run python main.py
```

### 6.4 项目结构

```
├─ main.py                     # 程序入口（locale 设置）
├─ pyproject.toml              # 项目配置与依赖声明
├─ modules/
│   ├─ gui.py                  # 图形界面（文字图片化渲染 + 动态参数）
│   ├─ text_renderer.py        # 中文文字→图片渲染引擎
│   ├─ services.py             # 统一调度层
│   ├─ filters.py              # 7类21种图像处理算法
│   ├─ face_detection.py       # Haar Cascade 人脸检测
│   ├─ beauty.py               # 人脸美化算法
│   ├─ beautygan.py            # BeautyGAN 妆造迁移（懒加载）
│   ├─ image_io.py             # 图像读写（支持中文路径）
│   ├─ utils.py                # 工具函数
│   └─ template_manager.py     # 妆造模板加载
├─ assets/
│   ├─ fonts/                  # 项目自带中文字体 NotoSansSC
│   └─ makeup_templates/       # 4种内置妆造模板
└─ docs/                       # 文档